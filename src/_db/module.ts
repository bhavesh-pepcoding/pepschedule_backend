import { Module } from '@nestjs/common';
import { ConfigService } from '@nestjs/config';
import { EyewitnessModule } from '@squareboat/nest-eyewitness';

@Module({
  imports: [
  ],
  controllers: [],
  providers: [],
})
export class DbModule { }
